apiVersion: v1
kind: Service
metadata:
  name: hello-world
spec:
  type: LoadBalancer
  selector:
    app: hello-world
  ports:
  - port: 8085           # ← you can change this freely (browser URL)
    targetPort: 8080     # ← MUST be 8080 (what the app actually listens on)